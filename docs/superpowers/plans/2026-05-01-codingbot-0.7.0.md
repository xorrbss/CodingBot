# CodingBot 0.7.0 — `serve` (W 사이클: 로컬 웹 대시보드) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 브라우저 한 화면에서 카운터 / judge 시계열 / last log / lock+stop alert를 동시에 모니터링하는 read-only 로컬 웹 대시보드를 추가한다.

**Architecture:** stdlib `http.server.ThreadingHTTPServer` + `BaseHTTPRequestHandler` 라우터. 정적 자원 1개(`codingbot/static/index.html`)는 vanilla HTML/JS/inline SVG 단일 파일. 클라이언트가 `setInterval`로 `/api/state` (1s), `/api/log/tail` (2s), `/api/timeline` (5s) 폴링. 서버는 `state.json` + `log.jsonl`만 read. 의존 0.

**Tech Stack:** Python 3.11 stdlib (`http.server`, `socketserver`, `threading`, `json`, `webbrowser`, `importlib.resources`), 기존 `codingbot.{state, paths}`. 프론트는 vanilla HTML/CSS/JS + inline SVG. pytest 8+, pytest-mock.

**Spec:** `docs/superpowers/specs/2026-05-01-codingbot-0.7.0-design.md`

**Base:** `v0.6.0` (commit `41d7b2b`).

---

## File Structure

| 파일 | 책임 | 신규/수정 | 목표 LOC |
|---|---|---|---|
| `codingbot/serve.py` | server entry + handler + timeline 집계 + lock/stop 헬퍼 | 신규 (Task 2-5) | ≤ 250 |
| `codingbot/static/__init__.py` | importlib.resources 패키지 마커 | 신규 (Task 1) | 1 |
| `codingbot/static/index.html` | 단일 페이지 UI (Layout B + 폴링 JS + SVG) | 신규 (Task 6) | ≤ 300 |
| `codingbot/cli.py` | `serve` subparser + `_cmd_serve` | 수정 (Task 7) | +10 |
| `tests/unit/test_serve.py` | 단위 (timeline / lock / handler routing / static traversal) | 신규 (Task 2-5) | — |
| `tests/unit/test_cli.py` | `serve` subparser default 검증 1건 추가 | 수정 (Task 7) | +1 |
| `tests/e2e/test_serve_lifecycle.py` | 실제 socket bind + urllib 호출 e2e_auto 1건 | 신규 (Task 8) | — |
| `pyproject.toml` | version 0.6.0 → 0.7.0. hatchling은 `codingbot/` 하위 비-py 파일을 default로 wheel에 포함 → 별도 selector 불필요 (Task 9에서 wheel 빌드로 검증) | 수정 (Task 9) | — |
| `docs/release-notes-0.7.0.md` | 릴리스 노트 | 신규 (Task 9) | — |
| `HANDOFF.md` | 0.7.0 ship 상태 | 수정 (Task 9) | — |
| `README.md` | `serve` 사용법 한 줄 추가 | 수정 (Task 9) | +2 |

빌드 backend는 hatchling. `[tool.hatch.build.targets.wheel] packages = ["codingbot"]` 그대로 두면 `codingbot/static/index.html`도 wheel에 포함된다. Task 9에서 `py -m build`(또는 `hatch build`)로 wheel을 풀어 실제 포함 여부를 한 번 검증한다.

---

## 공통 실행 환경

- 작업 디렉터리: `C:/project/CodingBot`
- 셸: Git Bash (Windows). 모든 경로 forward slash.
- pytest: `py -m pytest` (`.venv` 없음).
- 0.6.0까지의 conftest fixture: `tmp_codingbot_home`(격리 home), `mocker`(pytest-mock).
- e2e marker: `e2e_auto`(자동, fake claude 불필요), `e2e`(실제 claude, 수동만). 0.7.0 신규 e2e는 `e2e_auto`.
- 모든 텍스트 read/write는 `encoding="utf-8"`.

---

## Tasks

### Task 1 — `static/` 패키지 스캐폴드 + import 가능 확인

**Files:**
- Create: `codingbot/static/__init__.py`
- Create: `codingbot/static/index.html` (placeholder, Task 6에서 본문 채움)

- [ ] **Step 1: `codingbot/static/__init__.py` 생성 (빈 파일)**

`importlib.resources.files("codingbot.static")`가 동작하려면 `static/`이 importable subpackage여야 한다.

```python
# codingbot/static/__init__.py
"""정적 자원 패키지. importlib.resources로 read-only 접근."""
```

- [ ] **Step 2: `codingbot/static/index.html` placeholder 생성**

```html
<!doctype html><meta charset="utf-8"><title>codingbot</title>
<p>placeholder — replaced in Task 6</p>
```

- [ ] **Step 3: import 동작 확인**

Run:
```bash
py -c "from importlib.resources import files; print(files('codingbot.static') / 'index.html')"
```
Expected: `codingbot/static/index.html` 절대 경로 출력 (예외 없음).

- [ ] **Step 4: Commit**

```bash
git add codingbot/static/__init__.py codingbot/static/index.html
git commit -m "scaffold(serve): codingbot/static/ subpackage + placeholder index.html"
```

---

### Task 2 — `_compute_timeline` (TDD)

**Files:**
- Create: `codingbot/serve.py` (모듈 헤더 + `_compute_timeline`만)
- Create: `tests/unit/test_serve.py`

- [ ] **Step 1: failing test 4건 작성**

`tests/unit/test_serve.py`:

```python
"""serve 모듈 단위 테스트."""
import json
from datetime import datetime, timedelta, timezone

from codingbot import paths


def _ts(offset_sec: int) -> str:
    """현재 UTC 기준 offset_sec(음수면 과거)의 ISO8601(Z) 문자열."""
    t = datetime.now(timezone.utc) + timedelta(seconds=offset_sec)
    return t.isoformat(timespec="seconds").replace("+00:00", "Z")


def _line(event: str, **fields) -> str:
    offset = fields.pop("_offset", 0)
    record = {**fields, "ts": _ts(offset), "level": "info", "event": event}
    return json.dumps(record, ensure_ascii=False)


def test_compute_timeline_buckets_judge_events(tmp_codingbot_home):
    """judge=llm event(approve/defer/stop_hook)와 llm_timeout/llm_api_error를 60s bucket에 집계."""
    from codingbot import serve
    lines = [
        _line("auto_approve", judge="llm", reason="ok", _offset=-30),
        _line("auto_approve", judge="llm", reason="ok", _offset=-30),
        _line("auto_defer_to_user", judge="llm", reason="ask", _offset=-90),
        _line("stop_hook", judge="llm", outcome="continue", _offset=-150),
        _line("llm_timeout", _offset=-30),
        _line("llm_api_error", _offset=-90),
        _line("auto_approve", judge="heuristic", reason="safe", _offset=-30),  # heuristic은 제외
    ]
    paths.log_file().write_text("\n".join(lines) + "\n", encoding="utf-8")

    buckets = serve._compute_timeline(window_sec=300, bucket_sec=60)

    assert len(buckets) == 5  # 300/60
    # 마지막 bucket(=현재): approve 2 + timeout 1
    assert buckets[-1]["judge_call"] == 2
    assert buckets[-1]["judge_timeout"] == 1
    assert buckets[-1]["judge_error"] == 0
    # bucket[-2] (60~120s 전): defer 1 + api_error 1
    assert buckets[-2]["judge_call"] == 1
    assert buckets[-2]["judge_error"] == 1


def test_compute_timeline_ignores_old_lines(tmp_codingbot_home):
    from codingbot import serve
    # window 밖 (-1000s)
    paths.log_file().write_text(
        _line("auto_approve", judge="llm", _offset=-1000) + "\n", encoding="utf-8",
    )
    buckets = serve._compute_timeline(window_sec=300, bucket_sec=60)
    assert sum(b["judge_call"] for b in buckets) == 0


def test_compute_timeline_skips_malformed_lines(tmp_codingbot_home):
    from codingbot import serve
    paths.log_file().write_text(
        "not-json\n"
        + _line("auto_approve", judge="llm", _offset=-30) + "\n"
        + "{also bad\n",
        encoding="utf-8",
    )
    buckets = serve._compute_timeline(window_sec=120, bucket_sec=60)
    assert buckets[-1]["judge_call"] == 1


def test_compute_timeline_empty_log(tmp_codingbot_home):
    from codingbot import serve
    # log 파일 자체가 없음
    assert not paths.log_file().exists()
    buckets = serve._compute_timeline(window_sec=180, bucket_sec=60)
    assert len(buckets) == 3
    assert all(b["judge_call"] == 0 and b["judge_timeout"] == 0 and b["judge_error"] == 0 for b in buckets)
```

- [ ] **Step 2: Run failing tests**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 4 errors (`ImportError: cannot import name 'serve'`).

- [ ] **Step 3: `codingbot/serve.py` 신규 + `_compute_timeline` 구현**

```python
"""로컬 read-only 웹 대시보드.

데이터 소스: state.json + log.jsonl (read-only).
의존: stdlib만.
"""
import json
from datetime import datetime, timedelta, timezone
from typing import List, Optional

from codingbot import paths

# log.jsonl event 분류
_JUDGE_LLM_EVENTS = {"auto_approve", "auto_defer_to_user", "stop_hook"}


def _parse_ts(s: str) -> Optional[datetime]:
    """ISO8601(Z) → aware datetime. 실패 시 None."""
    try:
        if s.endswith("Z"):
            s = s[:-1] + "+00:00"
        return datetime.fromisoformat(s)
    except (ValueError, TypeError):
        return None


def _compute_timeline(window_sec: int, bucket_sec: int = 60) -> List[dict]:
    """`log.jsonl`을 한 번 read해서 judge 메트릭을 시간 bucket으로 집계.

    반환: `[{"t": iso8601, "judge_call": int, "judge_timeout": int, "judge_error": int}, ...]`
    bucket 수 = window_sec // bucket_sec. 빈 bucket도 0으로 채움.
    오래된 라인 / 깨진 라인 / log 파일 부재 모두 안전.
    """
    n_buckets = window_sec // bucket_sec
    now = datetime.now(timezone.utc)
    # bucket[i]는 [now - (n_buckets-i)*bucket_sec, now - (n_buckets-i-1)*bucket_sec)
    buckets = [
        {
            "t": (now - timedelta(seconds=(n_buckets - i - 1) * bucket_sec)).isoformat(timespec="seconds").replace("+00:00", "Z"),
            "judge_call": 0,
            "judge_timeout": 0,
            "judge_error": 0,
        }
        for i in range(n_buckets)
    ]

    p = paths.log_file()
    if not p.exists():
        return buckets

    cutoff = now - timedelta(seconds=window_sec)
    for raw in p.read_text(encoding="utf-8").splitlines():
        try:
            rec = json.loads(raw)
        except (ValueError, TypeError):
            continue
        ts = _parse_ts(rec.get("ts", ""))
        if ts is None or ts < cutoff:
            continue
        # bucket index: 가장 오래된 = 0, 가장 최근 = n_buckets-1
        delta = (now - ts).total_seconds()
        idx = n_buckets - 1 - int(delta // bucket_sec)
        if idx < 0 or idx >= n_buckets:
            continue
        event = rec.get("event")
        if event in _JUDGE_LLM_EVENTS and rec.get("judge") == "llm":
            buckets[idx]["judge_call"] += 1
        elif event == "llm_timeout":
            buckets[idx]["judge_timeout"] += 1
        elif event == "llm_api_error":
            buckets[idx]["judge_error"] += 1

    return buckets
```

- [ ] **Step 4: Run tests, expect green**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add codingbot/serve.py tests/unit/test_serve.py
git commit -m "feat(serve): _compute_timeline (60s bucket, judge_call/timeout/error)"
```

---

### Task 3 — `_read_lock_pid` + `_read_stop_signal` (TDD)

**Files:**
- Modify: `codingbot/serve.py`
- Modify: `tests/unit/test_serve.py`

- [ ] **Step 1: failing test 추가**

`tests/unit/test_serve.py`에 추가:

```python
def test_read_lock_pid_returns_int_when_file_exists(tmp_codingbot_home):
    from codingbot import serve
    paths.lock_file().write_text("13422", encoding="utf-8")
    assert serve._read_lock_pid() == 13422


def test_read_lock_pid_returns_none_when_missing(tmp_codingbot_home):
    from codingbot import serve
    assert serve._read_lock_pid() is None


def test_read_lock_pid_returns_none_for_corrupt_content(tmp_codingbot_home):
    from codingbot import serve
    paths.lock_file().write_text("not-a-pid", encoding="utf-8")
    assert serve._read_lock_pid() is None


def test_read_stop_signal_reflects_file_presence(tmp_codingbot_home):
    from codingbot import serve
    assert serve._read_stop_signal() is False
    paths.stop_signal_file().touch()
    assert serve._read_stop_signal() is True
```

- [ ] **Step 2: Run failing tests**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 4 errors (AttributeError on `_read_lock_pid` / `_read_stop_signal`).

- [ ] **Step 3: 구현 추가 (`codingbot/serve.py` 끝에)**

```python
def _read_lock_pid() -> Optional[int]:
    """`paths.lock_file()` 의 PID를 int로. 부재 또는 비숫자면 None."""
    p = paths.lock_file()
    if not p.exists():
        return None
    try:
        return int(p.read_text(encoding="utf-8").strip())
    except (ValueError, OSError):
        return None


def _read_stop_signal() -> bool:
    """`paths.stop_signal_file()` 존재 여부."""
    return paths.stop_signal_file().exists()
```

- [ ] **Step 4: Run tests**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 8 passed.

- [ ] **Step 5: Commit**

```bash
git add codingbot/serve.py tests/unit/test_serve.py
git commit -m "feat(serve): _read_lock_pid / _read_stop_signal helpers"
```

---

### Task 4 — `_Handler` 라우팅 (in-process WSGI-less, TDD)

**Files:**
- Modify: `codingbot/serve.py`
- Modify: `tests/unit/test_serve.py`

핸들러 자체를 e2e처럼 socket bind 없이 단위 테스트하기 위해, 라우팅 분기는 순수 함수 `_route(method, path)` → `(status, content_type, body_bytes)`로 분리한다. `_Handler.do_GET/do_POST`는 `_route`를 호출해 응답하는 얇은 래퍼.

- [ ] **Step 1: failing test 작성**

`tests/unit/test_serve.py`에 추가:

```python
def test_route_root_returns_index_html(tmp_codingbot_home):
    from codingbot import serve
    status, ctype, body = serve._route("GET", "/")
    assert status == 200
    assert ctype.startswith("text/html")
    # placeholder든 본문이든 비지 않음
    assert len(body) > 0


def test_route_static_index_html(tmp_codingbot_home):
    from codingbot import serve
    status, ctype, body = serve._route("GET", "/static/index.html")
    assert status == 200
    assert ctype.startswith("text/html")


def test_route_static_path_traversal_blocked(tmp_codingbot_home):
    from codingbot import serve
    status, _, _ = serve._route("GET", "/static/../../etc/passwd")
    assert status == 404


def test_route_static_unknown_file_returns_404(tmp_codingbot_home):
    from codingbot import serve
    status, _, _ = serve._route("GET", "/static/does-not-exist.css")
    assert status == 404


def test_route_api_state_returns_json(tmp_codingbot_home):
    from codingbot import state, serve
    state.start_cycle()
    status, ctype, body = serve._route("GET", "/api/state")
    assert status == 200
    assert ctype == "application/json"
    payload = json.loads(body.decode("utf-8"))
    # 0.6.0 카운터 키 일부 포함
    assert "cycles_this_run" in payload
    assert "judge_call_total" in payload
    # serve가 덧붙이는 키
    assert "lock_pid" in payload
    assert "stop_signal" in payload
    assert "ts" in payload


def test_route_api_log_tail_returns_lines(tmp_codingbot_home):
    from codingbot import serve
    paths.log_file().write_text(
        '{"event":"a"}\n{"event":"b"}\n{"event":"c"}\n', encoding="utf-8"
    )
    status, ctype, body = serve._route("GET", "/api/log/tail?n=2")
    assert status == 200
    assert ctype == "application/json"
    payload = json.loads(body.decode("utf-8"))
    assert payload["lines"] == ['{"event":"b"}', '{"event":"c"}']


def test_route_api_log_tail_default_n(tmp_codingbot_home):
    from codingbot import serve
    paths.log_file().write_text("\n".join(f'{{"i":{i}}}' for i in range(80)) + "\n", encoding="utf-8")
    status, _, body = serve._route("GET", "/api/log/tail")
    payload = json.loads(body.decode("utf-8"))
    # default n = 50
    assert len(payload["lines"]) == 50


def test_route_api_timeline_default_window(tmp_codingbot_home):
    from codingbot import serve
    status, ctype, body = serve._route("GET", "/api/timeline")
    assert status == 200
    assert ctype == "application/json"
    payload = json.loads(body.decode("utf-8"))
    # default window 1800s, bucket 60s = 30
    assert "buckets" in payload
    assert len(payload["buckets"]) == 30


def test_route_unknown_path_404(tmp_codingbot_home):
    from codingbot import serve
    status, _, _ = serve._route("GET", "/nope")
    assert status == 404


def test_route_non_get_returns_405(tmp_codingbot_home):
    from codingbot import serve
    status, _, _ = serve._route("POST", "/api/state")
    assert status == 405
```

- [ ] **Step 2: Run failing tests**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 10 errors / failures (AttributeError on `_route`).

- [ ] **Step 3: 구현 추가 (`codingbot/serve.py`)**

상단 import 보강:

```python
import re
from datetime import datetime, timedelta, timezone
from importlib.resources import files
from typing import List, Optional, Tuple
from urllib.parse import urlparse, parse_qs

from codingbot import paths, state
```

함수 추가 (파일 하단):

```python
_STATIC_NAME_RE = re.compile(r"^[A-Za-z0-9._-]+$")


def _read_log_tail_lines(n: int) -> List[str]:
    p = paths.log_file()
    if not p.exists():
        return []
    return p.read_text(encoding="utf-8").splitlines()[-n:]


def _load_static(name: str) -> Optional[bytes]:
    if not _STATIC_NAME_RE.match(name):
        return None
    target = files("codingbot.static") / name
    if not target.is_file():
        return None
    return target.read_bytes()


def _content_type_for(name: str) -> str:
    if name.endswith(".html"):
        return "text/html; charset=utf-8"
    if name.endswith(".js"):
        return "application/javascript; charset=utf-8"
    if name.endswith(".css"):
        return "text/css; charset=utf-8"
    return "application/octet-stream"


def _payload_state() -> bytes:
    s = state.read()
    enriched = {
        **s,
        "lock_pid": _read_lock_pid(),
        "stop_signal": _read_stop_signal(),
        "ts": datetime.now(timezone.utc).isoformat(timespec="seconds").replace("+00:00", "Z"),
    }
    return json.dumps(enriched, ensure_ascii=False).encode("utf-8")


def _payload_log_tail(n: int) -> bytes:
    return json.dumps({"lines": _read_log_tail_lines(n)}, ensure_ascii=False).encode("utf-8")


def _payload_timeline(window_sec: int) -> bytes:
    return json.dumps(
        {"buckets": _compute_timeline(window_sec=window_sec, bucket_sec=60)},
        ensure_ascii=False,
    ).encode("utf-8")


def _route(method: str, path: str) -> Tuple[int, str, bytes]:
    """순수 라우팅 함수. (status, content_type, body) 반환."""
    if method != "GET":
        return 405, "text/plain; charset=utf-8", b"method not allowed"

    parsed = urlparse(path)
    p = parsed.path
    qs = parse_qs(parsed.query)

    if p == "/":
        body = _load_static("index.html") or b""
        if not body:
            return 404, "text/plain; charset=utf-8", b"index not found"
        return 200, _content_type_for("index.html"), body

    if p.startswith("/static/"):
        name = p[len("/static/"):]
        body = _load_static(name)
        if body is None:
            return 404, "text/plain; charset=utf-8", b"not found"
        return 200, _content_type_for(name), body

    if p == "/api/state":
        return 200, "application/json", _payload_state()

    if p == "/api/log/tail":
        try:
            n = int(qs.get("n", ["50"])[0])
        except ValueError:
            n = 50
        return 200, "application/json", _payload_log_tail(n)

    if p == "/api/timeline":
        try:
            window = int(qs.get("window", ["1800"])[0])
        except ValueError:
            window = 1800
        return 200, "application/json", _payload_timeline(window)

    return 404, "text/plain; charset=utf-8", b"not found"
```

- [ ] **Step 4: Run tests**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 18 passed.

- [ ] **Step 5: Commit**

```bash
git add codingbot/serve.py tests/unit/test_serve.py
git commit -m "feat(serve): _route — /, /static/*, /api/state, /api/log/tail, /api/timeline"
```

---

### Task 5 — `_Handler` 래퍼 + `run_serve` 진입

**Files:**
- Modify: `codingbot/serve.py`

이 task는 socket bind / threading / browser 자동 오픈을 묶는 layer라 단위 회귀가 어렵다. 회귀는 Task 8의 e2e_auto가 담당.

- [ ] **Step 1: `_Handler` + `run_serve` 추가 (`codingbot/serve.py` 끝)**

```python
import sys
import threading
import webbrowser
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer


class _Handler(BaseHTTPRequestHandler):
    """라우팅은 순수 함수 _route에 위임. 이 클래스는 응답 write만 한다."""

    def _respond(self, status: int, content_type: str, body: bytes) -> None:
        self.send_response(status)
        self.send_header("Content-Type", content_type)
        self.send_header("Content-Length", str(len(body)))
        self.send_header("Cache-Control", "no-cache")
        self.end_headers()
        self.wfile.write(body)

    def do_GET(self) -> None:  # noqa: N802
        status, ctype, body = _route("GET", self.path)
        self._respond(status, ctype, body)

    def do_POST(self) -> None:  # noqa: N802
        status, ctype, body = _route("POST", self.path)
        self._respond(status, ctype, body)

    def log_message(self, format, *args) -> None:  # noqa: A002
        # default는 stderr에 access log를 찍음. 운영자 콘솔이 시끄러워져 끔.
        return


def _open_browser(host: str, port: int) -> None:
    try:
        webbrowser.open(f"http://{host}:{port}/")
    except Exception:
        pass


def run_serve(host: str, port: int, open_browser: bool) -> int:
    """블로킹 모드로 서버 시작. Ctrl-C → 0. port 충돌 → 1."""
    try:
        server = ThreadingHTTPServer((host, port), _Handler)
    except OSError as e:
        print(f"[codingbot] cannot bind {host}:{port} — {e}", file=sys.stderr)
        return 1

    if open_browser:
        threading.Thread(target=_open_browser, args=(host, port), daemon=True).start()

    print(f"[codingbot] serving on http://{host}:{port}  (Ctrl-C to stop)")
    try:
        server.serve_forever()
    except KeyboardInterrupt:
        pass
    finally:
        server.server_close()
    return 0
```

- [ ] **Step 2: import 동작 + LOC 확인**

Run:
```bash
py -c "from codingbot import serve; print('ok', len(open('codingbot/serve.py', encoding='utf-8').readlines()), 'lines')"
```
Expected: `ok N lines` (N ≤ 250).

- [ ] **Step 3: 단위 회귀 재실행**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 18 passed (Task 4 회귀 그대로 green).

- [ ] **Step 4: Commit**

```bash
git add codingbot/serve.py
git commit -m "feat(serve): _Handler + run_serve (ThreadingHTTPServer, browser auto-open)"
```

---

### Task 6 — `static/index.html` 본문 (Layout B + 폴링 JS + SVG)

**Files:**
- Modify: `codingbot/static/index.html`

placeholder를 실제 단일 페이지로 교체. vanilla JS로 1s/2s/5s 폴링, inline SVG로 timeline.

- [ ] **Step 1: `codingbot/static/index.html` 전체 교체**

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>CodingBot Dashboard</title>
  <style>
    :root { color-scheme: light dark; }
    body {
      font-family: ui-monospace, "JetBrains Mono", Menlo, Consolas, monospace;
      margin: 0; padding: 12px; font-size: 13px; line-height: 1.4;
    }
    .alert {
      position: sticky; top: 0; z-index: 10;
      padding: 8px 12px; border-radius: 4px; margin-bottom: 12px;
      background: rgba(0,0,0,0.06); border: 1px solid rgba(0,0,0,0.12);
    }
    .alert.warn { background: rgba(204, 0, 0, 0.15); border-color: rgba(204, 0, 0, 0.5); }
    .alert.disconnected { background: rgba(102, 102, 102, 0.2); border-color: rgba(102, 102, 102, 0.6); }
    .grid { display: grid; grid-template-columns: 2fr 3fr; gap: 12px; }
    .card { padding: 10px 12px; border: 1px solid rgba(0,0,0,0.15); border-radius: 4px; background: rgba(255,255,255,0.04); }
    .card h3 { margin: 0 0 6px 0; font-size: 11px; text-transform: uppercase; opacity: 0.7; font-weight: 600; }
    .card .v { font-size: 18px; font-weight: 700; }
    .card + .card { margin-top: 8px; }
    .right { display: flex; flex-direction: column; gap: 8px; }
    .log { max-height: 360px; overflow: auto; font-size: 11px; opacity: 0.85; white-space: pre; }
    svg.timeline { width: 100%; height: 80px; display: block; }
    .legend { font-size: 10px; opacity: 0.65; margin-top: 4px; }
    .legend .sw { display: inline-block; width: 10px; height: 10px; vertical-align: middle; margin-right: 3px; border-radius: 2px; }
  </style>
</head>
<body>
  <div id="alert" class="alert">connecting…</div>

  <div class="grid">
    <div class="left">
      <div class="card">
        <h3>Cycle</h3>
        <div class="v" id="cycles">–</div>
        <div>auto_approve <span id="auto_approve">–</span> · auto_continue <span id="auto_continue">–</span></div>
      </div>
      <div class="card">
        <h3>PreToolUse</h3>
        <div>approve heur <span id="ap_heur">–</span> / llm <span id="ap_llm">–</span></div>
        <div>defer rule <span id="df_rule">–</span> / heur <span id="df_heur">–</span> / llm <span id="df_llm">–</span></div>
      </div>
      <div class="card">
        <h3>Stop</h3>
        <div>continue <span id="st_cont">–</span> · handoff <span id="st_hand">–</span> · unstuck <span id="st_unst">–</span> · allow <span id="st_allow">–</span></div>
      </div>
      <div class="card">
        <h3>Judge</h3>
        <div>call <span id="j_call">–</span> · timeout <span id="j_to">–</span> · error <span id="j_err">–</span></div>
      </div>
    </div>

    <div class="right">
      <div class="card">
        <h3>Judge timeline (last 30 min)</h3>
        <svg class="timeline" id="tl" viewBox="0 0 300 80" preserveAspectRatio="none"></svg>
        <div class="legend">
          <span class="sw" style="background:#6aa84f"></span>call
          <span class="sw" style="background:#e69138"></span>timeout
          <span class="sw" style="background:#cc0000"></span>error
        </div>
      </div>
      <div class="card">
        <h3>Last log</h3>
        <div class="log" id="log">–</div>
      </div>
    </div>
  </div>

<script>
const $ = (id) => document.getElementById(id);

async function fetchJSON(url) {
  const r = await fetch(url, { cache: "no-store" });
  if (!r.ok) throw new Error(r.status);
  return r.json();
}

function renderAlert(s) {
  const a = $("alert");
  if (!s) {
    a.className = "alert disconnected";
    a.textContent = "disconnected — retrying…";
    return;
  }
  const lock = s.lock_pid != null ? `runner lock: pid ${s.lock_pid}` : "runner lock: none";
  const stop = s.stop_signal ? "stop signal: YES" : "stop signal: no";
  a.textContent = `${stop} · ${lock} · ${s.ts}`;
  a.className = "alert" + (s.stop_signal || s.lock_pid != null ? " warn" : "");
}

function renderState(s) {
  $("cycles").textContent = s.cycles_this_run ?? 0;
  $("auto_approve").textContent = s.auto_approve_count ?? 0;
  $("auto_continue").textContent = s.auto_continue_count ?? 0;
  $("ap_heur").textContent = s.auto_approve_by_heuristic ?? 0;
  $("ap_llm").textContent = s.auto_approve_by_llm ?? 0;
  $("df_rule").textContent = s.auto_defer_by_rule ?? 0;
  $("df_heur").textContent = s.auto_defer_by_heuristic ?? 0;
  $("df_llm").textContent = s.auto_defer_by_llm ?? 0;
  $("st_cont").textContent = s.stop_block_continue ?? 0;
  $("st_hand").textContent = s.stop_block_handoff ?? 0;
  $("st_unst").textContent = s.stop_block_unstuck ?? 0;
  $("st_allow").textContent = s.stop_allow ?? 0;
  $("j_call").textContent = s.judge_call_total ?? 0;
  $("j_to").textContent = s.judge_timeout_total ?? 0;
  $("j_err").textContent = s.judge_error_total ?? 0;
}

function renderLog(payload) {
  $("log").textContent = (payload.lines || []).join("\n") || "(empty)";
}

function renderTimeline(payload) {
  const svg = $("tl");
  svg.innerHTML = "";
  const buckets = payload.buckets || [];
  const n = buckets.length || 1;
  const w = 300 / n;
  let max = 1;
  for (const b of buckets) {
    const total = b.judge_call + b.judge_timeout + b.judge_error;
    if (total > max) max = total;
  }
  buckets.forEach((b, i) => {
    const x = i * w;
    const heights = [
      { v: b.judge_call,    color: "#6aa84f" },
      { v: b.judge_timeout, color: "#e69138" },
      { v: b.judge_error,   color: "#cc0000" },
    ];
    let yCursor = 80;
    for (const seg of heights) {
      if (seg.v <= 0) continue;
      const h = (seg.v / max) * 80;
      const r = document.createElementNS("http://www.w3.org/2000/svg", "rect");
      r.setAttribute("x", x.toFixed(2));
      r.setAttribute("y", (yCursor - h).toFixed(2));
      r.setAttribute("width", (w * 0.9).toFixed(2));
      r.setAttribute("height", h.toFixed(2));
      r.setAttribute("fill", seg.color);
      svg.appendChild(r);
      yCursor -= h;
    }
  });
}

async function tickState() {
  try {
    const s = await fetchJSON("/api/state");
    renderAlert(s);
    renderState(s);
  } catch (e) { renderAlert(null); }
}
async function tickLog() {
  try { renderLog(await fetchJSON("/api/log/tail?n=50")); } catch (e) {}
}
async function tickTimeline() {
  try { renderTimeline(await fetchJSON("/api/timeline?window=1800")); } catch (e) {}
}

tickState(); tickLog(); tickTimeline();
setInterval(tickState, 1000);
setInterval(tickLog, 2000);
setInterval(tickTimeline, 5000);
</script>
</body>
</html>
```

- [ ] **Step 2: LOC 확인**

Run: `wc -l codingbot/static/index.html`
Expected: 줄 수 ≤ 300.

- [ ] **Step 3: 단위 회귀 (정적 read 경로 변동 확인)**

Run: `py -m pytest tests/unit/test_serve.py -q`
Expected: 18 passed (placeholder가 본문으로 바뀌어도 `body 길이 > 0` 단언이 그대로 유지됨).

- [ ] **Step 4: Commit**

```bash
git add codingbot/static/index.html
git commit -m "feat(serve): index.html — Layout B (sticky alert + 2-column + SVG timeline)"
```

---

### Task 7 — `cli.py`에 `serve` subcommand 연결

**Files:**
- Modify: `codingbot/cli.py`
- Modify: `tests/unit/test_cli.py`

- [ ] **Step 1: failing test 추가**

`tests/unit/test_cli.py` 끝에:

```python
# 0.7.0 — `serve` (W 사이클)

def test_serve_subparser_invokes_run_serve_with_defaults(tmp_codingbot_home, mocker):
    """`codingbot serve`가 host=127.0.0.1, port=8723, open_browser=True로 run_serve 호출."""
    spy = mocker.patch("codingbot.serve.run_serve", return_value=0)
    rc = cli.main(["serve"])
    assert rc == 0
    spy.assert_called_once_with("127.0.0.1", 8723, True)


def test_serve_subparser_overrides(tmp_codingbot_home, mocker):
    spy = mocker.patch("codingbot.serve.run_serve", return_value=0)
    rc = cli.main(["serve", "--host", "0.0.0.0", "--port", "9000", "--no-browser"])
    assert rc == 0
    spy.assert_called_once_with("0.0.0.0", 9000, False)
```

- [ ] **Step 2: Run failing tests**

Run: `py -m pytest tests/unit/test_cli.py -q`
Expected: 2 errors (no `serve` subparser).

- [ ] **Step 3: `codingbot/cli.py` 수정**

`_cmd_status` 위 또는 `_cmd_config` 아래에 함수 추가:

```python
def _cmd_serve(args: argparse.Namespace) -> int:
    from codingbot.serve import run_serve
    return run_serve(args.host, args.port, not args.no_browser)
```

`_build_parser` 안에서 `cfg = sub.add_parser("config", ...)` 다음 줄에 subparser 추가:

```python
serve = sub.add_parser("serve", help="로컬 웹 대시보드 시작")
serve.add_argument("--host", default="127.0.0.1", help="bind host. default 127.0.0.1")
serve.add_argument("--port", type=int, default=8723, help="bind port. default 8723")
serve.add_argument("--no-browser", action="store_true", help="자동 브라우저 오픈 비활성")
serve.set_defaults(func=_cmd_serve)
```

- [ ] **Step 4: Run tests**

Run: `py -m pytest tests/unit/test_cli.py -q`
Expected: 모두 passed (기존 + 신규 2건).

- [ ] **Step 5: Commit**

```bash
git add codingbot/cli.py tests/unit/test_cli.py
git commit -m "feat(cli): add `codingbot serve` subcommand"
```

---

### Task 8 — e2e_auto: 실제 socket bind + urllib 호출

**Files:**
- Create: `tests/e2e/test_serve_lifecycle.py`

- [ ] **Step 1: 테스트 작성**

```python
"""serve 통합 e2e: 실제 socket bind + threading + urllib client.

별도 fake_claude나 hook은 필요 없음. ANTHROPIC_API_KEY도 불필요.
"""
import json
import socket
import threading
import time
import urllib.request

import pytest

pytestmark = pytest.mark.e2e_auto


def _free_port() -> int:
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
        s.bind(("127.0.0.1", 0))
        return s.getsockname()[1]


def _wait_until_serving(url: str, timeout_sec: float = 5.0) -> None:
    deadline = time.time() + timeout_sec
    last_err = None
    while time.time() < deadline:
        try:
            with urllib.request.urlopen(url, timeout=0.5) as r:
                if r.status == 200:
                    return
        except Exception as e:
            last_err = e
            time.sleep(0.05)
    raise AssertionError(f"server never came up: {last_err}")


def test_serve_lifecycle_get_state_and_shutdown(tmp_codingbot_home):
    from codingbot import serve, state
    state.start_cycle()  # state.json이 실제 존재하도록

    port = _free_port()
    rc_holder = {}

    def _run():
        rc_holder["rc"] = serve.run_serve("127.0.0.1", port, open_browser=False)

    th = threading.Thread(target=_run, daemon=True)
    th.start()
    try:
        base = f"http://127.0.0.1:{port}"
        _wait_until_serving(f"{base}/api/state")

        # /api/state
        with urllib.request.urlopen(f"{base}/api/state", timeout=2) as r:
            assert r.status == 200
            payload = json.loads(r.read().decode("utf-8"))
        assert "cycles_this_run" in payload
        assert "lock_pid" in payload
        assert "stop_signal" in payload

        # /api/timeline
        with urllib.request.urlopen(f"{base}/api/timeline?window=600", timeout=2) as r:
            assert r.status == 200
            tl = json.loads(r.read().decode("utf-8"))
        assert "buckets" in tl
        assert len(tl["buckets"]) == 10

        # / (index.html)
        with urllib.request.urlopen(f"{base}/", timeout=2) as r:
            assert r.status == 200
            ctype = r.headers.get("Content-Type", "")
            assert ctype.startswith("text/html")
            body = r.read()
        assert b"<title>CodingBot Dashboard</title>" in body

        # 404
        try:
            urllib.request.urlopen(f"{base}/nope", timeout=2)
            raise AssertionError("expected 404")
        except urllib.error.HTTPError as e:
            assert e.code == 404
    finally:
        # KeyboardInterrupt를 메인 스레드에서 raise해도 worker thread의 serve_forever는 안 멈춘다.
        # 직접 server reference를 노출하지 않으므로 process-level 종료를 흉내내기 위해
        # 같은 host:port에 대한 fresh 요청을 모두 끝낸 뒤 daemon=True로 두어 프로세스 종료 시 자동 정리.
        # 단, 즉시 종료를 위해 별도 _shutdown 헬퍼를 호출해야 한다면 serve 모듈에 shutdown api를 노출해야 한다.
        # 0.7.0에서는 이 테스트가 daemon thread 정리에 의존한다 (pytest 종료 시 회수).
        pass
```

- [ ] **Step 2: Run e2e_auto**

Run: `py -m pytest tests/e2e/test_serve_lifecycle.py -q -m e2e_auto`
Expected: 1 passed.

- [ ] **Step 3: 만약 실패하면 — server shutdown api 추가**

`run_serve`가 server 인스턴스를 닫을 외부 핸들을 노출하지 않으면 daemon thread cleanup이 충분하다. 그러나 OS별로 socket TIME_WAIT 영향으로 다음 테스트 port 충돌이 발생하면, 다음 변경을 `codingbot/serve.py`에 추가하고 테스트의 finally에서 호출:

```python
# serve.py — module-level
_active_server = None  # type: Optional[ThreadingHTTPServer]

def shutdown_active() -> None:
    global _active_server
    if _active_server is not None:
        _active_server.shutdown()
        _active_server.server_close()
        _active_server = None
```

`run_serve` 안에서 `server` 만든 직후 `_active_server = server`, finally에서 `_active_server = None` 추가. 테스트 finally에서 `serve.shutdown_active()` 호출.

- 이 변경을 추가했다면 단위 테스트(Task 4)는 영향 없음. 회귀 재실행만 필요.

- [ ] **Step 4: 단위 회귀 재실행**

Run: `py -m pytest tests/unit -q`
Expected: 모두 passed.

- [ ] **Step 5: Commit**

```bash
git add tests/e2e/test_serve_lifecycle.py codingbot/serve.py
git commit -m "test(e2e): serve lifecycle (socket bind + state/timeline/index 응답)"
```

(`codingbot/serve.py`에 변경이 없다면 그것만 빼고 add.)

---

### Task 9 — release 0.7.0

**Files:**
- Modify: `pyproject.toml`
- Create: `docs/release-notes-0.7.0.md`
- Modify: `HANDOFF.md`
- Modify: `README.md`

- [ ] **Step 1: 전체 회귀 grand pass**

Run: `py -m pytest -q`
Expected: 모두 passed (단위 + e2e_auto). e2e(수동)는 제외/skip.

Run:
```bash
grep -rn "BLOCKED" codingbot tests
```
Expected: no match.

Run:
```bash
wc -l codingbot/*.py codingbot/static/index.html | sort -n
```
Expected: 모두 ≤ 500. `codingbot/serve.py` ≤ 250, `codingbot/static/index.html` ≤ 300.

- [ ] **Step 2: `pyproject.toml` version bump**

```diff
- version = "0.6.0"
+ version = "0.7.0"
```

- [ ] **Step 3: wheel 빌드 → static 포함 검증**

Run:
```bash
py -m pip install --quiet build
py -m build --wheel
py -c "import zipfile; print('\n'.join(n for n in zipfile.ZipFile(sorted(__import__('glob').glob('dist/codingbot-0.7.0-*.whl'))[-1]).namelist() if 'static' in n))"
```
Expected: `codingbot/static/__init__.py` 와 `codingbot/static/index.html` 두 줄 출력.

만약 출력이 비면, hatchling이 비-py를 누락한 것이므로 `pyproject.toml`에 다음 추가:

```toml
[tool.hatch.build.targets.wheel.force-include]
"codingbot/static" = "codingbot/static"
```

다시 build → 재검증.

- [ ] **Step 4: `docs/release-notes-0.7.0.md` 작성**

```markdown
# CodingBot 0.7.0

## What's new

- **`codingbot serve`** — 로컬 read-only 웹 대시보드. 카운터, judge 시계열(30분/60s bucket), 최근 log 50줄, lock+stop alert를 한 화면에서 본다.
  - default `http://127.0.0.1:8723`. `--port`, `--host`, `--no-browser` 지원.
  - 의존 추가 0개 (stdlib `http.server` + vanilla HTML/JS/SVG).
  - 데이터 소스: 기존 `~/.codingbot/state.json` + `log.jsonl` 만 read.

## Compatibility

- 기존 명령(`run`, `stop`, `start`, `status`, `tail-log`, `install-hooks`, `uninstall-hooks`, `config`) 동작 동일.
- `status --watch` (0.6.0)는 그대로 동작. `serve`와 공존.
- state schema, log 포맷 변경 없음.

## Notes

- 인증/원격 노출 없음. `--host 0.0.0.0`은 사용자 책임.
- 모든 endpoint는 GET. 쓰기 API 없음.
```

- [ ] **Step 5: `README.md`에 사용법 두 줄 추가**

`codingbot status --watch ...` 예시 다음에:

```
codingbot serve                       # 브라우저에서 라이브 대시보드 (default: http://127.0.0.1:8723)
codingbot serve --port 9000 --no-browser
```

- [ ] **Step 6: `HANDOFF.md` 업데이트**

"지금까지 한 일"에 0.7.0 항목 추가. "다음 후보" 섹션에서 0.7.0 W 사이클 항목 제거. "이어가는 방법" 베이스를 0.6.0 → 0.7.0으로 동기화.

- [ ] **Step 7: 사용자 확인 요청 (commit + tag + push)**

사용자 확인 후:

```bash
git add pyproject.toml docs/release-notes-0.7.0.md HANDOFF.md README.md
git commit -m "release: 0.7.0 — codingbot serve (W 사이클: 로컬 웹 대시보드)"
git tag -a v0.7.0 -m "0.7.0 — codingbot serve"
git push origin master
git push origin v0.7.0
```

---

## Risk

- **hatchling이 비-py 파일을 wheel에 누락할 가능성** — Task 9-Step 3에서 검증. 누락 시 `force-include` 추가.
- **e2e_auto에서 server thread cleanup 부족** — 다음 테스트 port 충돌 가능. Task 8-Step 3의 `shutdown_active()` 추가로 해결.
- **`_compute_timeline`이 큰 log.jsonl 전부 read** — 0.6.0 `_read_log_tail`과 동일 트레이드오프. 회전 정책이 들어오면 다음 사이클에서 tail-style read로 갈아탄다.
- **브라우저 자동 오픈이 headless 환경에서 stderr 노이즈** — `_open_browser`의 `try/except: pass`로 봉합. CI는 `--no-browser` 사용.
- **`/api/state`가 `state.read()` 캐시되지 않은 채 1초마다 디스크 read** — state.json은 작아서 IO 부담 무시 가능. measurement는 0.7.x 외.

---

## Spec Coverage Self-Check

| 스펙 §   | 계획 task | 검증 |
|---|---|---|
| §3.1.1 run_serve 진입 | Task 5 | Task 8 e2e |
| §3.1.2 라우팅 (5 endpoints) | Task 4 | Task 4 + Task 8 e2e |
| §3.1.3 helpers (lock/stop/timeline) | Task 2-3 | Task 2-3 단위 |
| §3.1.4 importlib.resources static 로드 | Task 1, Task 4 | Task 4 단위 |
| §3.2 index.html (Layout B) | Task 6 | Task 8 e2e (`<title>` 포함) |
| §3.3 cli serve subcommand | Task 7 | Task 7 단위 2건 |
| §3.4 패키징 (wheel 포함) | Task 9-Step 3 | wheel 내용 inspection |
| §3.5 paths 변경 없음 | (변경 없음) | — |
| §4.1 단위 7건 | Task 2/3/4에 분산, 합계 18 | 단위 회귀 |
| §4.2 e2e 1건 | Task 8 | e2e_auto |
| §4.3 cli 회귀 | Task 7 | 단위 회귀 |
| §6 비범위 | (구현 안 함) | — |
| §7 완료 조건 | Task 9-Step 1 (회귀/BLOCKED grep/LOC), Step 3 (wheel), Step 6 (HANDOFF) | — |
